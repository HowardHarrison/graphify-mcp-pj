# Prefer project-root `.env` (copy from `../.env.example`).
# This file remains supported for local script overrides.

GRAPH_PATH=graph/graph.json
MCP_HOST=127.0.0.1
MCP_PORT=8765
MCP_PATH=/mcp

# Optional:
# GRAPHIFY_API_KEY=
