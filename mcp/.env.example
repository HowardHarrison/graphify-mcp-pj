# Graph path relative to the project root (or absolute)
GRAPH_PATH=graph/graph.json

# Local-only HTTP bind settings for Graphify MCP
MCP_HOST=127.0.0.1
MCP_PORT=8765
MCP_PATH=/mcp

# Optional: require an API key on the HTTP transport
# GRAPHIFY_API_KEY=
