# Optional Docker / MCP environment overrides.
# Copy to .env and adjust if needed. Do not commit real secrets.

# Optional API key for Graphify MCP HTTP transport inside Docker
# GRAPHIFY_API_KEY=

# Documented ports (see docker-compose.yml)
# Host MCP endpoint: http://127.0.0.1:8765/mcp
# Container internal: http://0.0.0.0:8080/mcp
